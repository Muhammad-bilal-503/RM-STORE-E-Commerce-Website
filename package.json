{
  "name": "rm-store",
  "version": "1.0.0",
  "description": "RM STORE E-commerce Website (monorepo dev convenience scripts — see DEPLOYMENT.md for Azure)",
  "private": true,
  "scripts": {
    "server": "npm run dev --prefix backend",
    "client": "npm start --prefix frontend",
    "admin": "npm start --prefix admin/admin-frontend",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-deps": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin/admin-frontend",
    "build": "npm run build --prefix frontend"
  },
  "keywords": [
    "ecommerce",
    "food",
    "mern"
  ],
  "author": "RM STORE",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}

